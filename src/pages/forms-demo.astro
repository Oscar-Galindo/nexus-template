---
import ContactForm from '@/components/forms/ContactForm';
import QuoteForm from '@/components/forms/QuoteForm';
import PrayerRequestForm from '@/components/forms/PrayerRequestForm';
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Forms Demo - Nexus Template</title>
  </head>
  <body class="bg-gray-50 py-12 px-4">
    <div class="max-w-4xl mx-auto">
      <header class="text-center mb-12">
        <h1 class="text-4xl font-bold text-gray-900 mb-4">Forms Demo</h1>
        <p class="text-lg text-gray-600">
          Provider-agnostic form components that work with GHL or email notifications
        </p>
        <div class="mt-4 p-4 bg-blue-50 border border-blue-200 rounded-lg inline-block">
          <p class="text-sm text-blue-800">
            <strong>Current Provider:</strong> <code class="bg-blue-100 px-2 py-1 rounded">{import.meta.env.FORM_PROVIDER || 'ghl'}</code>
          </p>
        </div>
      </header>

      <!-- Contact Form Section -->
      <section class="mb-16 bg-white rounded-lg shadow-md p-8">
        <div class="mb-6">
          <h2 class="text-2xl font-bold text-gray-900 mb-2">Contact Form</h2>
          <p class="text-gray-600">General purpose contact form for business or church sites.</p>
        </div>
        <ContactForm client:load />
      </section>

      <!-- Quote Form Section -->
      <section class="mb-16 bg-white rounded-lg shadow-md p-8">
        <div class="mb-6">
          <h2 class="text-2xl font-bold text-gray-900 mb-2">Quote Request Form</h2>
          <p class="text-gray-600">Business form with service selection and budget options.</p>
        </div>
        <QuoteForm client:load />
      </section>

      <!-- Prayer Request Form Section -->
      <section class="mb-16 bg-white rounded-lg shadow-md p-8">
        <div class="mb-6">
          <h2 class="text-2xl font-bold text-gray-900 mb-2">Prayer Request Form</h2>
          <p class="text-gray-600">Church form with privacy and pastoral care options.</p>
        </div>
        <PrayerRequestForm client:load />
      </section>

      <!-- Documentation Section -->
      <footer class="text-center text-gray-600 mt-12">
        <h3 class="text-xl font-semibold mb-4">How It Works</h3>
        <div class="grid md:grid-cols-3 gap-6 text-left">
          <div class="bg-white p-6 rounded-lg shadow">
            <div class="text-3xl mb-3">📝</div>
            <h4 class="font-semibold mb-2">1. Fill Form</h4>
            <p class="text-sm">User fills out any of the forms above with their information.</p>
          </div>
          <div class="bg-white p-6 rounded-lg shadow">
            <div class="text-3xl mb-3">🔄</div>
            <h4 class="font-semibold mb-2">2. Submit to API</h4>
            <p class="text-sm">Form data is sent to <code class="bg-gray-100 px-1">/api/submit-form</code> endpoint.</p>
          </div>
          <div class="bg-white p-6 rounded-lg shadow">
            <div class="text-3xl mb-3">✅</div>
            <h4 class="font-semibold mb-2">3. Process by Provider</h4>
            <p class="text-sm">Automatically routed to GHL CRM or email notification.</p>
          </div>
        </div>

        <div class="mt-8 p-6 bg-gray-100 rounded-lg">
          <h4 class="font-semibold mb-3">Switch Providers</h4>
          <p class="text-sm mb-2">Change your <code class="bg-white px-2 py-1 rounded">FORM_PROVIDER</code> environment variable:</p>
          <div class="space-y-2 text-left max-w-md mx-auto">
            <div class="bg-white p-3 rounded">
              <code class="text-sm">FORM_PROVIDER=ghl</code>
              <span class="text-gray-500 ml-2">→ GoHighLevel CRM</span>
            </div>
            <div class="bg-white p-3 rounded">
              <code class="text-sm">FORM_PROVIDER=simple</code>
              <span class="text-gray-500 ml-2">→ Email via Resend</span>
            </div>
          </div>
          <p class="text-sm mt-3 text-gray-600">No code changes needed! 🎉</p>
        </div>
      </footer>
    </div>
  </body>
</html>
